# Changelog

## 1.0.1

Renamed every action to use descriptive "Verb + Noun" labels that follow
Zapier's naming conventions. Action keys are unchanged, so existing Zaps keep
working.

1. Update create/extract label to "Extract Data"
2. Update create/screenshot label to "Create Screenshot"
3. Update create/pdf label to "Generate PDF"
4. Update create/markdown label to "Convert Page to Markdown"
5. Update create/text label to "Extract Text"
6. Update create/audio label to "Detect Audio"
7. Update create/video label to "Detect Video"
8. Update create/insights label to "Get Insights"
9. Update create/logo label to "Get Logo"
10. Fix connection label: removed the integration-name fallback so Zapier
    auto-numbers connections (no sensitive value exposed).

## 1.0.0

Initial release.
